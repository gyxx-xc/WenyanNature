# 编码习惯蒸馏报告

## 样本范围
- **语言**：Java（379文件）、Python（7文件）
- **核心样本**：`judou/`（文言编译器，117文件）、`language_processor/`（注解处理器，8文件）
- **本人基线样本**：`judou/` + `language_processor/`（高置信度单人项目）
- **多人协作样本**：`src/`（Minecraft Mod，254文件）存在小部分多人协作可能，但风格与基线高度一致
- **排除样本**：构建产物、虚拟环境、生成资源、第三方库
- **可靠性判断**：高 - 基线样本风格一致，未发现明显AI或协作污染
- **AI介入样本**：未发现明显AI强辅助区域
- **本人加AI样本**：无
- **可疑协作样本**：未发现明显第二套人类作者风格簇

## 特征总表
> 每条特征说明：特征本身、归因对象、信号强度、置信度、证据来源、是否纳入模仿

- **接口命名使用'I'前缀**：归因对象：用户偏好；信号强度：强；置信度：高；证据：`judou/src/main/java`中24个接口以I开头（如`IWenyanValue`, `IWenyanFunction`）；纳入策略：纳入
- **类名使用领域前缀（Wenyan）**：归因对象：用户偏好；信号强度：强；置信度：高；证据：跨`judou/`和`src/`模块稳定使用`Wenyan`前缀（`WenyanInteger`, `WenyanCompiler`, `WenyanFrame`）；纳入策略：纳入
- **使用记录类（record）而非传统POJO**：归因对象：用户偏好（Java 17+特性）；信号强度：强；置信度：高；证据：`judou/`中19处record使用（`WenyanString`, `WenyanPackage`等）；纳入策略：纳入
- **注释密度中等，偏重Javadoc风格**：归因对象：用户偏好；信号强度：强；置信度：高；证据：`judou/`平均注释密度5.5行/文件，`src/`平均2.2行/文件；核心库注释更多；纳入策略：纳入
- **使用switch表达式（Java 17+）而非传统switch**：归因对象：用户偏好；信号强度：中；置信度：高；证据：`judou/`中10处switch使用，多为表达式风格；`src/CodeField.java`中使用switch表达式处理键盘事件；纳入策略：纳入
- **错误处理使用自定义异常链**：归因对象：用户偏好；信号强度：强；置信度：高；证据：`WenyanException`及其子类（`WenyanNumberException`, `WenyanVarException`）；方法声明`throws`；纳入策略：纳入
- **使用JetBrains注解（@NotNull, @Nullable）**：归因对象：用户偏好；信号强度：强；置信度：高；证据：遍布`judou/`和`language_processor/`；纳入策略：纳入
- **局部变量命名语义清晰，无拼音混用**：归因对象：用户偏好；信号强度：强；置信度：高；证据：`value`, `bytecode`, `environment`, `result`等语义化命名；英文质量良好；纳入策略：纳入
- **测试使用JUnit 5参数化测试**：归因对象：用户偏好；信号强度：强；置信度：高；证据：`judou/test_statement/`和`src/test/`中使用`@ParameterizedTest` + `@MethodSource`/`@CsvSource`；纳入策略：纳入
- **包结构按功能分层**：归因对象：用户偏好；信号强度：强；置信度：高；证据：`judou/`分层（`compiler`, `structure/values`, `runtime`, `utils`）；`src/`分层（`client`, `setup`, `content`, `interpreter_impl`）；纳入策略：纳入
- **使用Lombok注解（@Getter, @Setter）**：归因对象：用户偏好（在Minecraft Mod中）；信号强度：中；置信度：高；证据：`src/`中GUI类使用`@Getter`/`@Setter`；纳入策略：纳入（但仅在GUI/DTO场景）
- **使用枚举实现单例或工具类**：归因对象：用户偏好；信号强度：中；置信度：高；证据：`WenyanPreprocessor`, `ChineseUtils`为枚举；纳入策略：纳入

## 频率概览
- **注释频率**：核心库（`judou/`）注释密度更高（5.5行/文件），业务代码（`src/`）较低（2.2行/文件）；注释多为Javadoc风格，解释类/方法职责；行内注释较少，主要用于TODO/HACK标记；信号强度：强；置信度：高；证据：统计扫描
- **常用内容频率**：高频术语：`value`, `bytecode`, `runtime`, `frame`, `compiler`, `visitor`, `handler`, `packet`, `backend`；高频职责切法：接口-实现分离，值对象使用record，工具类用枚举；错误处理模板：`throws WenyanException` + `try-catch`特定异常；信号强度：强；置信度：高；证据：跨文件复现

## 写法归因
- **接口I前缀**：用户个人偏好，非Java社区强制规范；信号强度：强；置信度：高；证据：稳定复现于`judou/`全部接口；模仿策略：保留
- **switch表达式**：用户偏好Java 17+特性，非外部API约束；信号强度：中；置信度：高；证据：多处使用switch表达式而非语句；模仿策略：保留
- **记录类（record）**：用户偏好Java 17+特性，用于简单数据载体；信号强度：强；置信度：高；证据：值对象几乎全用record；模仿策略：保留
- **JetBrains注解**：用户偏好静态分析辅助，非框架要求；信号强度：强；置信度：高；证据：广泛使用；模仿策略：保留
- **Lombok注解**：在GUI/DTO层使用，非全局偏好；信号强度：中；置信度：高；证据：仅`src/`中部分类使用；模仿策略：仅在类似场景保留

## 来源分类
- **用户本人稳定手写**：`judou/`全部，`language_processor/`全部，`src/`的大部分业务代码
- **用户本人 + AI 强辅助**：未发现
- **其他人类协作者**：可能存在小部分（`src/`中某些模块），但风格未明显断裂
- **模板 / 自动生成 / 第三方**：`src/generated/`资源文件，构建产物，不用于风格提炼

## 协作者簇
未发现明显第二套稳定风格簇。`src/`整体风格与`judou/`高度一致，表明可能单人主导或紧密协作。

## Java画像
### 高置信度习惯
1. **命名规则**：接口以I前缀，类名使用领域前缀（Wenyan），局部变量语义清晰（value, bytecode等）；信号强度：强；置信度：高；证据：跨模块复现；模仿规则：继续写同类代码时保持相同命名风格
2. **类型选择**：优先使用record而非class表示不可变数据，使用enum实现工具类/单例；信号强度：强；置信度：高；证据：`judou/`中19处record，多处enum；模仿规则：简单值对象用record，无状态工具类考虑enum
3. **控制流**：偏好switch表达式（Java 17+），避免传统switch语句；信号强度：中；置信度：高；证据：10+处switch表达式；模仿规则：处理多分支时优先用switch表达式
4. **错误处理**：定义自定义异常层次（WenyanException子类），方法显式声明throws，特定异常用try-catch捕获；信号强度：强；置信度：高；证据：异常类设计，方法签名；模仿规则：定义领域异常，显式声明throws，精细捕获
5. **注解使用**：广泛使用JetBrains `@NotNull`/`@Nullable`，GUI/DTO层使用Lombok `@Getter`/`@Setter`；信号强度：强；置信度：高；证据：遍布代码；模仿规则：参数/返回值加nullability注解，GUI类用Lombok简化访问器
6. **测试组织**：JUnit 5参数化测试，测试类继承助手类，测试方法以`test`前缀命名；信号强度：强；置信度：高；证据：多个测试模块；模仿规则：写测试时用`@ParameterizedTest`，提取通用逻辑到父类
7. **包结构**：按功能/职责分层，而非按技术层；信号强度：强；置信度：高；证据：`judou/`（compiler, structure, runtime, utils），`src/`（client, setup, content）；模仿规则：按模块职责划分包，而非严格MVC

### 命名语言质量
- **观察**：标准英文命名，无拼音、中文直译或错拼英文混用；局部变量语义清晰（value, bytecode, environment等），无tmp/data/obj泛名滥用；信号强度：强；置信度：高；证据：全代码库扫描；模仿策略：保持标准英文命名，避免拼音/中文混入

### 函数内部细粒度偏好
- **局部变量**：语义清晰，偏向完整单词（value, result, bytecode），而非单字母或泛名
- **控制流**：switch表达式处理离散分支，if-else用于条件判断，循环多用for/while而非stream API
- **临时对象/中间状态**：使用局部变量存储中间结果，而非链式调用；复杂状态用对象封装

### 代表性代码片段
（详见representative-snippets.md）

### 频率特征
- **注释频率**：核心库注释密度高（5.5行/文件），业务代码较低（2.2行/文件）；注释多用于类/方法Javadoc，行内注释仅用于TODO/HACK；信号强度：强；置信度：高；证据：统计；模仿规则：核心/公共API写完整Javadoc，业务逻辑注释稀疏
- **常用内容频率**：高频术语反映领域模型（value, bytecode, runtime, frame等）；信号强度：强；置信度：高；证据：词频分析；模仿规则：在相同领域使用一致术语

### 弱信号
- **Python脚本风格**：样本不足（7文件），仅见生成脚本；信号强度：弱；置信度：低；证据：有限样本；模仿策略：不提炼Python专属习惯，用Python社区常规补全
- **资源文件组织**：JSON/TOML/模型文件多为生成或配置，不体现编码风格；信号强度：弱；置信度：低；证据：非代码文件；模仿策略：忽略

### 禁止误迁移
1. **Lombok使用**：仅在GUI/DTO场景出现，不要迁移到核心算法/数据结构代码
2. **注释密度**：业务代码注释稀疏是项目特点，非绝对规则，新代码可适度增加解释性注释
3. **Java版本特性**：record/switch表达式需Java 17+，若目标环境不支持需调整

## Python画像
### 样本不足说明
仅7个Python文件（生成脚本、文档工具），不足以提炼语言专属习惯。观察到的有限模式：
- 脚本式风格，函数为主
- 使用标准库（json, re, os）
- 无类定义，无复杂抽象

### 模仿策略
样本不足，不提炼Python专属习惯。需模仿时，按Python社区常规写法补全。

## 模仿规则摘要
1. **接口I前缀，类名领域前缀**
2. **值对象用record，工具类考虑enum**
3. **错误处理用自定义异常，显式throws**
4. **偏好switch表达式，JetBrains注解**
5. **测试用JUnit 5参数化，继承助手类**
6. **包按功能分层，非严格技术分层**
7. **核心库注释多，业务代码注释稀疏**
8. **命名标准英文，无拼音/中文混用**

## 详细总结
用户编码风格呈现以下特点：
- **技术栈**：Java 17+特性熟练（record, switch表达式），JetBrains注解，Lombok（有限场景）
- **架构倾向**：清晰分层，接口-实现分离，自定义异常层次
- **代码密度**：中等注释，核心库文档更完整
- **命名质量**：标准英文，语义清晰，无拼音/中文混用
- **测试习惯**：JUnit 5参数化测试，重用测试助手
- **项目一致性**：`judou/`（编译器）与`src/`（Mod）风格高度一致，表明单人主导或紧密协作

风格中**用户个人偏好**部分：I前缀接口、record偏好、switch表达式、自定义异常、JetBrains注解。
风格中**外部API/框架约束**部分：Minecraft NeoForge框架约束（`src/`）、JUnit 5约定。
风格中**语言常规**部分：Java命名规范、包结构基础。

未发现明显AI强辅助或协作者污染，风格一致性高。

## 能力边界
- **作者稳定会写**：Java编译器/运行时、Minecraft Mod、注解处理器、单元测试
- **疑似需AI强辅助**：未发现（无风格断裂、无过度注释、无异常规整代码）
- **疑似来自他人协作**：可能小部分`src/`模块，但风格未明显断裂
- **判断依据**：全量扫描，风格一致性分析，无第二套稳定命名/注释/抽象体系

## 诚实边界
- **Java样本充足**，可提炼高置信度习惯
- **Python样本严重不足**，不提炼语言专属习惯
- **注释密度差异**：`judou/`（5.5）vs `src/`（2.2）反映项目类型差异，非绝对规则
- **Lombok使用局限**：仅在GUI/DTO层，非全局偏好
- **未发现AI/协作污染**，但小部分`src/`代码可能有他人贡献，风格未断裂故不影响主画像