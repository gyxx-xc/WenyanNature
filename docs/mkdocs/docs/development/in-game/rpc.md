# remote executor

to remote exec the function, just like the current import that return string.
but it's run on the source fu instead of currently run on local fu.

## process

1. compile
2. run and return function
3. accept function

## usage

```
import pakcage.

package(package.function)
```
